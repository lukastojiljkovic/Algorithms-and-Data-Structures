#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
#include <cmath>
#include <map>
#include <queue>
#include <iomanip>
#include <unordered_map>
#include <vector>
#include <stack>
#include <utility>
#include <numeric>
using namespace std;

void sledeci_binarni(vector<char>& binary) {
    
}

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    
    char x;
    vector<char> binary;
    while(cin >> x) {
        binary.push_back(x);
    }
    sledeci_binarni(binary);

    return 0;
}
